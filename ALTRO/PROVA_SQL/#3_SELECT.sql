-- seleziona colonna1, colonna2, ... FROM(dalla) nome_tabella
-- SELECT colona1, colonna2, ... FROM nome_tabella

-- seleziona *(tutte le collone) FROM(dalla) nome_tabella
-- SELECT * FROM nome_tabella

-- seleziona colonna denominazione dalla tabella clienti
-- SELECT denominazione FROM clienti

-- seleziona la collona denominazione, p_iva dalla tabella clienti (non e obbligatorio mantenere l'ordine delle colonne)
-- SELECT denominazione, p_iva FROM clienti
